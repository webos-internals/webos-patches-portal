/*
 * Registration.feature
 * Step definitions for Feature: 'Registration'
 *
 * Auto-generated using Kyuri: http://github.com/nodejitsu/kyuri
 */
 
var kyuri = require('kyuri'),
    Steps = require('kyuri').Steps;

//
// Step definitions for Scenario: Successful Registration
//
Steps.Given(/^GIVEN I have entered my first name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your Given code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my last name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my email address$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my password$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.When(/^WHEN I press register$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your When code here. */
    
    return topic;
  };
});

Steps.Then(/^THEN the resulting screen should say "Thank you for registering"$/, function (topic) {
  return function () {
    /* Put your assert messages for this Then here */
  };
});

//
// Step definitions for Scenario: Missing First Name
//
Steps.Given(/^GIVEN I have not entered my first name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your Given code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my last name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my email address$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my password$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.When(/^WHEN I press register$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your When code here. */
    
    return topic;
  };
});

Steps.Then(/^THEN the resulting screen should say "Missing first name"$/, function (topic) {
  return function () {
    /* Put your assert messages for this Then here */
  };
});

//
// Step definitions for Scenario: Missing Last Name
//
Steps.Given(/^GIVEN I have entered my first name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your Given code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have not entered my last name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my email address$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my password$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.When(/^WHEN I press register$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your When code here. */
    
    return topic;
  };
});

Steps.Then(/^THEN the resulting screen should say "Missing last name"$/, function (topic) {
  return function () {
    /* Put your assert messages for this Then here */
  };
});

//
// Step definitions for Scenario: Missing Email Address
//
Steps.Given(/^GIVEN I have entered my first name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your Given code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my last name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have not entered my email address$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my password$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.When(/^WHEN I press register$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your When code here. */
    
    return topic;
  };
});

Steps.Then(/^THEN the resulting screen should say "Missing email address"$/, function (topic) {
  return function () {
    /* Put your assert messages for this Then here */
  };
});

//
// Step definitions for Scenario: Weak password
//
Steps.Given(/^GIVEN I have entered my first name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your Given code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my last name$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered my email address$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.And(/^AND I have entered a password less than 6 characters$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your And code here. */
    
    return topic;
  };
});

Steps.When(/^WHEN I press register$/, function (topic) {
  return function () {
    // Always use or extend the same topic since you don't 
    // know how nested or not nested you are at this point
    topic = topic || {};
    
    /* Put your When code here. */
    
    return topic;
  };
});

Steps.Then(/^THEN the resulting screen should say "Password is too weak"$/, function (topic) {
  return function () {
    /* Put your assert messages for this Then here */
  };
});

Steps.export(module);